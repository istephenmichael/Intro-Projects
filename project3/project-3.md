Update ubuntu

`sudo apt update`

Upgrade ubuntu

`sudo apt upgrade`

Install Node.js on the server

`sudo apt-get install -y nodejs`

Verify the node installation

`node -v`

Verify the node installation

`npm -v` 
